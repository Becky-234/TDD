1. Set up folder
1. Create virtual enviroment `python3 -m venv .venv `
1. Activate virtual environment `source .venv/bin/activate`
1. Install pytest `pip install pytest`
1. Install pytest-watch `pip install pytest-watch`
1. Run pytest `pytest`
1. Run pytest-watch `ptw`



 If the number is odd, it outputs Fizz
 If the number is even, it outputs Buzz
